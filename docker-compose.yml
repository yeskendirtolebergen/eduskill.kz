version: "3.9"

services:
  postgres:
    image: postgres:15
    container_name: eduskill_postgres
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: eduskill
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    volumes:
      - eduskill_pgdata:/var/lib/postgresql/data

volumes:
  eduskill_pgdata:
